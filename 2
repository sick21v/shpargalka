#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main() {

	double a = 0.0, b = 0.0;
	double result = 0.0;

	scanf("%lf", &a);
	scanf("%lf", &b);

	result = (a * a + b * b) <= 4;

	printf("result = %s\n", result ? "true" : "false");
	return 0;
}
